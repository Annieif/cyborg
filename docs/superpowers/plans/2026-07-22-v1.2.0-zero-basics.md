# v1.2.0 "零基础版" 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让零基础、无编程环境的用户也能一键部署并使用 Minecraft AI 赛博人

**Architecture:** Docker 一键部署（docker-compose up）取代手动安装 Node.js/npm；Web 端引导式配置向导取代手动编辑 .env 文件；内置智能默认值实现零配置启动；一键部署到 Railway/Render 等云平台

**Tech Stack:** Docker, docker-compose, React (SetupWizard 组件), Express (配置保存 API), Node.js fs (运行时写入 .env)

---

## 文件结构

```
赛博人/
├── docker-compose.yml          # 🔧 增强：所有默认值内联，无需外部 .env
├── Dockerfile                   # 🔧 增强：多架构构建 + 健康检查
├── .dockerignore               # 🆕 新建：优化构建上下文
├── deploy/                     # 🆕 新建：云平台部署模板
│   ├── railway.json            # Railway 一键部署配置
│   └── render.yaml             # Render 一键部署配置
├── scripts/                     # 🆕 新建：构建脚本
│   ├── bundle-assets.js         # 前端资源打包为 TypeScript 模块
│   ├── build-binary.sh          # Linux/Mac 可执行文件构建
│   └── build-binary.ps1         # Windows 可执行文件构建
├── .github/
│   └── workflows/
│       └── release-binaries.yml # 🆕 新建：自动化多平台构建
├── src/
│   ├── frontendAssets.ts        # 🆕 新建：开发模式空 fallback（构建时覆盖）
│   ├── config/
│   │   └── index.ts            
│   └── server/
│       └── index.ts            # 🔧 增强：二进制模式内存静态文件服务
├── frontend/
│   └── src/
│       ├── App.tsx              # 🔧 增强：首次运行显示 SetupWizard
│       ├── components/
│       │   ├── SetupWizard.tsx  # 🆕 新建：引导式配置向导
│       │   └── SetupWizard.css  # 🆕 新建：向导样式
│       └── main.tsx             # 不变
├── docs/
│   └── superpowers/
│       └── plans/
│           └── 2026-07-22-v1.2.0-zero-basics.md  # 🆕 本文件
├── README.md                    # 🔧 重写：面向零基础用户
├── QUICKSTART.md               # 🆕 新建：三步快速开始指南
├── package.json                 # 🔧 版本号 → 1.2.0
└── .env.example                 # 🔧 更新：标注所有可省略的配置
```

---

### Task 1: Docker 一键部署增强

**Files:**
- Modify: `docker-compose.yml`
- Modify: `Dockerfile`
- Create: `.dockerignore`

**目标：** 用户只需 `docker-compose up` 无需任何前置配置即可启动

- [ ] **Step 1: docker-compose.yml 内置所有默认值**

```yaml
version: '3.8'

services:
  cyborg-bot:
    image: annieif/cyborg:latest  # 也可用 build: . 本地构建
    container_name: mc-ai-cyborg
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      # 必填：AI API Key（网页向导中填写，或这里直接设）
      - AI_API_KEY=${AI_API_KEY:-}
      # 必填：Minecraft 服务器地址
      - MC_HOST=${MC_HOST:-localhost}
      # 以下全部有默认值，可省略
      - MC_PORT=${MC_PORT:-25565}
      - MC_USERNAME=${MC_USERNAME:-AI_Cyborg}
      - MC_VERSION=${MC_VERSION:-1.20.1}
      - MC_AUTH=${MC_AUTH:-offline}
      - AI_PROVIDER=${AI_PROVIDER:-openai}
      - AI_MODEL=${AI_MODEL:-gpt-4o-mini}
      - AI_BASE_URL=${AI_BASE_URL:-https://api.openai.com/v1}
      - AI_PERSONA=${AI_PERSONA:-你是一个友善的Minecraft AI赛博人。你可以与玩家聊天、帮助他们建造、探索世界。你的名字是赛博人。}
      - AI_MAX_CONTEXT=${AI_MAX_CONTEXT:-20}
      - AI_MAX_TOKENS=${AI_MAX_TOKENS:-8000}
      - AI_TEMPERATURE=${AI_TEMPERATURE:-0.7}
      - AI_ACTION_CHAIN=${AI_ACTION_CHAIN:-true}
      - AI_VISION=${AI_VISION:-false}
      - AI_AUTONOMOUS=${AI_AUTONOMOUS:-false}
      - AI_MAX_TOOL_LOOPS=${AI_MAX_TOOL_LOOPS:-5}
      - WEB_PORT=3000
      - WEB_HOST=0.0.0.0
      - LOG_LEVEL=${LOG_LEVEL:-info}
      - LOG_FILE=logs/bot.log
    volumes:
      - ./logs:/app/logs
      - ./experience:/app/experience
      - ./.env:/app/.env  # 挂载以便 Web 向导写入
    networks:
      - cyborg-net

networks:
  cyborg-net:
    driver: bridge
```

- [ ] **Step 2: Dockerfile 多阶段构建优化**

```dockerfile
# 使用 Node.js 20 LTS（更新版本）
FROM node:20-alpine AS builder

WORKDIR /app
COPY package.json package-lock.json* ./
RUN npm ci --legacy-peer-deps
COPY tsconfig.json ./
COPY src/ ./src/
RUN npm run build

# 前端构建
FROM node:20-alpine AS frontend-builder
WORKDIR /app/frontend
COPY frontend/package.json frontend/package-lock.json* ./
RUN npm ci
COPY frontend/ ./
RUN npm run build

# 生产镜像
FROM node:20-alpine
WORKDIR /app

RUN addgroup -g 1001 -S cyborg && \
    adduser -u 1001 -S cyborg -G cyborg

COPY --from=builder /app/dist ./dist
COPY --from=builder /app/node_modules ./node_modules
COPY --from=builder /app/package.json ./
COPY --from=frontend-builder /app/frontend/dist ./frontend/dist

RUN mkdir -p logs experience && chown -R cyborg:cyborg /app

COPY .env.example .env.example

USER cyborg
EXPOSE 3000

HEALTHCHECK --interval=30s --timeout=5s --start-period=10s --retries=3 \
  CMD wget --no-verbose --tries=1 --spider http://localhost:3000/api/health || exit 1

CMD ["node", "dist/index.js"]
```

- [ ] **Step 3: 创建 .dockerignore**

```
node_modules
dist
frontend/dist
frontend/node_modules
logs
experience
.git
.gitignore
.env
*.md
!README.md
测试服务器
docs
```

- [ ] **Step 4: 构建并验证**

```bash
docker build -t cyborg-bot:test .
docker run --rm -p 3000:3000 -e AI_API_KEY=sk-test cyborg-bot:test
# 验证: curl http://localhost:3000/api/health 返回 200
```

---

### Task 2: Web 配置向导

**Files:**
- Create: `frontend/src/components/SetupWizard.tsx`
- Create: `frontend/src/components/SetupWizard.css`
- Modify: `frontend/src/App.tsx`
- Modify: `src/server/index.ts`

**目标：** 首次运行（无 .env 或 API Key 为空）时，自动弹出引导式配置向导

- [ ] **Step 1: 创建 SetupWizard 组件**

```tsx
// frontend/src/components/SetupWizard.tsx
import { useState } from 'react';
import './SetupWizard.css';

interface SetupConfig {
  aiProvider: 'openai' | 'claude' | 'custom' | 'ollama' | 'free';
  aiApiKey: string;
  aiModel: string;
  aiBaseUrl: string;
  mcHost: string;
  mcPort: number;
  mcUsername: string;
  mcVersion: string;
}

const STEPS = [
  { key: 'welcome', title: '欢迎', desc: '开始配置你的 Minecraft AI 赛博人' },
  { key: 'ai', title: 'AI 设置', desc: '选择 AI 后端' },
  { key: 'server', title: '服务器', desc: '连接 Minecraft 服务器' },
  { key: 'bot', title: '角色', desc: '设置赛博人身份' },
  { key: 'done', title: '完成', desc: '启动赛博人' },
];

const AI_PROVIDERS = [
  { value: 'openai', label: 'OpenAI (推荐)', desc: '需要 API Key，最稳定' },
  { value: 'free', label: 'ChatAnywhere 免费 API', desc: '无需付费，按请求次数限制' },
  { value: 'ollama', label: 'Ollama 本地模型', desc: '完全免费，需要本地部署' },
  { value: 'custom', label: '自定义 API', desc: '兼容 OpenAI 格式的 API' },
  { value: 'claude', label: 'Claude (Anthropic)', desc: '需要 API Key' },
];

const PRESET_SERVERS = [
  { value: '', label: '自定义服务器' },
  { value: 'hypixel', label: 'Hypixel (mc.hypixel.net)', host: 'mc.hypixel.net', port: 25565, version: '1.8.9' },
  { value: '2b2t', label: '2b2t (2b2t.org)', host: '2b2t.org', port: 25565, version: '1.12.2' },
];

export function SetupWizard({ onComplete }: { onComplete: (config: SetupConfig) => void }) {
  const [step, setStep] = useState(0);
  const [config, setConfig] = useState<SetupConfig>({
    aiProvider: 'free',
    aiApiKey: '',
    aiModel: 'gpt-4o-mini',
    aiBaseUrl: '',
    mcHost: 'localhost',
    mcPort: 25565,
    mcUsername: 'AI_Cyborg',
    mcVersion: '1.20.1',
  });
  const [saving, setSaving] = useState(false);
  const [error, setError] = useState('');

  const updateConfig = (partial: Partial<SetupConfig>) => {
    setConfig(prev => ({ ...prev, ...partial }));
    setError('');
  };

  const handleProviderChange = (provider: 'openai' | 'claude' | 'custom' | 'ollama' | 'free') => {
    const defaults: Record<string, Partial<SetupConfig>> = {
      openai: { aiBaseUrl: 'https://api.openai.com/v1', aiModel: 'gpt-4o-mini' },
      free: { aiBaseUrl: 'https://api.chatanywhere.tech/v1', aiModel: 'gpt-4o-mini', aiApiKey: '' },
      ollama: { aiBaseUrl: 'http://localhost:11434/v1', aiModel: 'llama3.2', aiApiKey: '' },
      custom: { aiBaseUrl: '', aiModel: 'gpt-4o-mini' },
      claude: { aiBaseUrl: 'https://api.anthropic.com/v1', aiModel: 'claude-sonnet-4-20250514' },
    };
    updateConfig({ aiProvider: provider, ...defaults[provider] });
  };

  const handleSave = async () => {
    setSaving(true);
    setError('');
    try {
      const res = await fetch('/api/config/save', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(config),
      });
      const data = await res.json();
      if (!data.success) throw new Error(data.error || '保存失败');
      onComplete(config);
    } catch (e: any) {
      setError(e.message || '保存配置失败，请检查网络连接');
    } finally {
      setSaving(false);
    }
  };

  const renderStep = () => {
    switch (step) {
      case 0: // Welcome
        return (
          <div className="wizard-step">
            <h2>欢迎使用 Minecraft AI 赛博人</h2>
            <p className="wizard-desc">只需 3 步，你的 AI 机器人就能在 Minecraft 中自由活动。</p>
            <div className="wizard-features">
              <div className="feature-card">
                <span className="feature-icon">🤖</span>
                <strong>AI 智能对话</strong>
                <p>接入 OpenAI/Claude/本地模型，自然语言交流</p>
              </div>
              <div className="feature-card">
                <span className="feature-icon">🌍</span>
                <strong>世界交互</strong>
                <p>挖掘、建造、合成、战斗，20+ 种操作</p>
              </div>
              <div className="feature-card">
                <span className="feature-icon">🎮</span>
                <strong>真人代理</strong>
                <p>通过 Web 面板直接操控赛博人</p>
              </div>
            </div>
          </div>
        );

      case 1: // AI Config
        return (
          <div className="wizard-step">
            <h2>选择 AI 后端</h2>
            <p className="wizard-desc">赛博人需要一个 AI 大脑来思考和对话</p>
            
            <div className="provider-list">
              {AI_PROVIDERS.map(p => (
                <label key={p.value} className={`provider-card ${config.aiProvider === p.value ? 'selected' : ''}`}>
                  <input
                    type="radio"
                    name="aiProvider"
                    value={p.value}
                    checked={config.aiProvider === p.value}
                    onChange={() => handleProviderChange(p.value as any)}
                  />
                  <div className="provider-info">
                    <strong>{p.label}</strong>
                    <span>{p.desc}</span>
                  </div>
                </label>
              ))}
            </div>

            {config.aiProvider !== 'free' && config.aiProvider !== 'ollama' && (
              <div className="form-group">
                <label>API Key</label>
                <input
                  type="password"
                  value={config.aiApiKey}
                  onChange={e => updateConfig({ aiApiKey: e.target.value })}
                  placeholder={config.aiProvider === 'openai' ? 'sk-...' : '输入你的 API Key'}
                />
              </div>
            )}

            <div className="form-group">
              <label>模型</label>
              <input
                type="text"
                value={config.aiModel}
                onChange={e => updateConfig({ aiModel: e.target.value })}
                placeholder="gpt-4o-mini"
              />
            </div>

            {config.aiProvider === 'custom' && (
              <div className="form-group">
                <label>API 地址</label>
                <input
                  type="text"
                  value={config.aiBaseUrl}
                  onChange={e => updateConfig({ aiBaseUrl: e.target.value })}
                  placeholder="https://your-api.com/v1"
                />
              </div>
            )}
          </div>
        );

      case 2: // Server Config
        return (
          <div className="wizard-step">
            <h2>连接 Minecraft 服务器</h2>
            <p className="wizard-desc">赛博人需要加入一个 Minecraft 服务器</p>

            <div className="form-group">
              <label>服务器地址</label>
              <select
                value={config.mcHost}
                onChange={e => {
                  const preset = PRESET_SERVERS.find(p => p.host === e.target.value);
                  if (preset) {
                    updateConfig({ mcHost: preset.host, mcPort: preset.port, mcVersion: preset.version });
                  } else {
                    updateConfig({ mcHost: e.target.value });
                  }
                }}
              >
                <option value="localhost">本地服务器 (localhost)</option>
                {PRESET_SERVERS.filter(p => p.value).map(p => (
                  <option key={p.value} value={p.host}>{p.label}</option>
                ))}
                <option value="">自定义...</option>
              </select>
            </div>

            {!PRESET_SERVERS.find(p => p.host === config.mcHost) && (
              <div className="form-group">
                <label>服务器地址</label>
                <input
                  type="text"
                  value={config.mcHost}
                  onChange={e => updateConfig({ mcHost: e.target.value })}
                  placeholder="例如: mc.example.com"
                />
              </div>
            )}

            <div className="form-row">
              <div className="form-group">
                <label>端口</label>
                <input
                  type="number"
                  value={config.mcPort}
                  onChange={e => updateConfig({ mcPort: parseInt(e.target.value) || 25565 })}
                />
              </div>
              <div className="form-group">
                <label>版本</label>
                <input
                  type="text"
                  value={config.mcVersion}
                  onChange={e => updateConfig({ mcVersion: e.target.value })}
                  placeholder="1.20.1"
                />
              </div>
            </div>
          </div>
        );

      case 3: // Bot Identity
        return (
          <div className="wizard-step">
            <h2>设置赛博人身份</h2>
            <p className="wizard-desc">给你的 AI 赛博人起个名字和性格</p>

            <div className="form-group">
              <label>游戏内名称</label>
              <input
                type="text"
                value={config.mcUsername}
                onChange={e => updateConfig({ mcUsername: e.target.value })}
                placeholder="AI_Cyborg"
              />
            </div>
          </div>
        );

      case 4: // Done
        return (
          <div className="wizard-step">
            <h2>准备就绪</h2>
            <p className="wizard-desc">检查以下配置，确认无误后启动</p>
            
            <div className="config-summary">
              <div className="summary-item">
                <span className="summary-label">AI 后端</span>
                <span className="summary-value">{AI_PROVIDERS.find(p => p.value === config.aiProvider)?.label}</span>
              </div>
              <div className="summary-item">
                <span className="summary-label">模型</span>
                <span className="summary-value">{config.aiModel}</span>
              </div>
              <div className="summary-item">
                <span className="summary-label">服务器</span>
                <span className="summary-value">{config.mcHost}:{config.mcPort}</span>
              </div>
              <div className="summary-item">
                <span className="summary-label">名称</span>
                <span className="summary-value">{config.mcUsername}</span>
              </div>
            </div>

            {error && <div className="wizard-error">{error}</div>}
          </div>
        );
    }
  };

  return (
    <div className="setup-wizard-overlay">
      <div className="setup-wizard">
        <div className="wizard-progress">
          {STEPS.map((s, i) => (
            <div key={s.key} className={`progress-step ${i < step ? 'done' : ''} ${i === step ? 'active' : ''}`}>
              <div className="step-dot">{i < step ? '✓' : i + 1}</div>
              <span className="step-title">{s.title}</span>
            </div>
          ))}
        </div>

        <div className="wizard-content">
          {renderStep()}
        </div>

        <div className="wizard-actions">
          {step > 0 && (
            <button className="btn-secondary" onClick={() => setStep(step - 1)}>上一步</button>
          )}
          <div className="wizard-actions-right">
            {step < 4 ? (
              <button className="btn-primary" onClick={() => setStep(step + 1)}>
                {step === 0 ? '开始配置' : '下一步'}
              </button>
            ) : (
              <button className="btn-primary" onClick={handleSave} disabled={saving}>
                {saving ? '保存中...' : '启动赛博人'}
              </button>
            )}
          </div>
        </div>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: 创建 SetupWizard.css**

```css
.setup-wizard-overlay {
  position: fixed; top: 0; left: 0; right: 0; bottom: 0;
  background: rgba(0,0,0,0.7); display: flex; align-items: center; justify-content: center;
  z-index: 1000;
}
.setup-wizard {
  background: #1a1a2e; border-radius: 16px; width: 640px; max-width: 90vw;
  max-height: 90vh; overflow-y: auto; padding: 32px; color: #e0e0e0;
}
.wizard-progress { display: flex; justify-content: space-between; margin-bottom: 32px; }
.progress-step { text-align: center; flex: 1; position: relative; }
.progress-step::after { content: ''; position: absolute; top: 12px; left: 60%; width: 80%; height: 2px; background: #333; }
.progress-step:last-child::after { display: none; }
.progress-step.done::after { background: #4caf50; }
.step-dot {
  width: 28px; height: 28px; border-radius: 50%; background: #333;
  display: flex; align-items: center; justify-content: center; margin: 0 auto 8px;
  font-size: 12px;
}
.progress-step.active .step-dot { background: #2196f3; }
.progress-step.done .step-dot { background: #4caf50; }
.step-title { font-size: 12px; color: #888; }
.progress-step.active .step-title { color: #fff; }
.progress-step.done .step-title { color: #4caf50; }
.wizard-step h2 { margin: 0 0 8px; font-size: 22px; }
.wizard-desc { color: #888; margin: 0 0 24px; }
.wizard-features { display: grid; grid-template-columns: repeat(3, 1fr); gap: 16px; margin: 24px 0; }
.feature-card { background: #16213e; border-radius: 12px; padding: 20px; text-align: center; }
.feature-card .feature-icon { font-size: 32px; display: block; margin-bottom: 8px; }
.feature-card strong { display: block; margin-bottom: 4px; }
.feature-card p { font-size: 13px; color: #888; margin: 0; }
.provider-list { display: flex; flex-direction: column; gap: 8px; margin-bottom: 20px; }
.provider-card {
  display: flex; align-items: center; gap: 12px; padding: 12px 16px;
  background: #16213e; border-radius: 8px; cursor: pointer; border: 2px solid transparent;
}
.provider-card.selected { border-color: #2196f3; background: #1a2744; }
.provider-card input { display: none; }
.provider-info strong { display: block; }
.provider-info span { font-size: 13px; color: #888; }
.form-group { margin-bottom: 16px; }
.form-group label { display: block; margin-bottom: 6px; font-size: 14px; color: #aaa; }
.form-group input, .form-group select {
  width: 100%; padding: 10px 12px; background: #16213e; border: 1px solid #333;
  border-radius: 8px; color: #fff; font-size: 14px; box-sizing: border-box;
}
.form-group input:focus, .form-group select:focus { outline: none; border-color: #2196f3; }
.form-row { display: flex; gap: 12px; }
.form-row .form-group { flex: 1; }
.config-summary { background: #16213e; border-radius: 8px; padding: 16px; margin: 16px 0; }
.summary-item { display: flex; justify-content: space-between; padding: 8px 0; border-bottom: 1px solid #333; }
.summary-item:last-child { border-bottom: none; }
.summary-label { color: #888; }
.summary-value { font-weight: 500; }
.wizard-error { color: #f44336; font-size: 14px; margin-top: 12px; padding: 8px; background: rgba(244,67,54,0.1); border-radius: 8px; }
.wizard-actions { display: flex; justify-content: space-between; margin-top: 24px; }
.wizard-actions-right { margin-left: auto; }
.btn-primary {
  padding: 10px 24px; background: #2196f3; color: #fff; border: none;
  border-radius: 8px; cursor: pointer; font-size: 14px; font-weight: 500;
}
.btn-primary:hover { background: #1976d2; }
.btn-primary:disabled { opacity: 0.5; cursor: not-allowed; }
.btn-secondary {
  padding: 10px 24px; background: transparent; color: #888; border: 1px solid #444;
  border-radius: 8px; cursor: pointer; font-size: 14px;
}
.btn-secondary:hover { border-color: #666; color: #fff; }
```

- [ ] **Step 3: 修改 App.tsx 集成 SetupWizard**

在 `App.tsx` 的 return 顶部添加：

```tsx
// 在 App 组件内添加状态
const [showWizard, setShowWizard] = useState(false);

// 在 useEffect 中检测是否首次运行
useEffect(() => {
  fetch('/api/config/check')
    .then(r => r.json())
    .then(data => {
      if (!data.configured) setShowWizard(true);
    });
}, []);

// 在 return 最外层添加
return (
  <>
    {showWizard && (
      <SetupWizard onComplete={() => setShowWizard(false)} />
    )}
    <div className="app">
      {/* ... 现有内容 ... */}
    </div>
  </>
);
```

- [ ] **Step 4: 后端新增 /api/config/* 路由**

在 `src/server/index.ts` 的 `setupRoutes()` 中添加：

```typescript
// === 配置向导 API ===
import fs from 'fs';
import path from 'path';

// 检查是否已配置
this.app.get('/api/config/check', (_req, res) => {
  const config = getConfig();
  const configured = config.ai.apiKey !== '' || config.ai.provider === 'free' || config.ai.provider === 'ollama';
  res.json({ configured });
});

// 保存配置到 .env 文件
this.app.post('/api/config/save', async (req, res) => {
  try {
    const { aiProvider, aiApiKey, aiModel, aiBaseUrl, mcHost, mcPort, mcUsername, mcVersion } = req.body;
    
    const envContent = [
      `# AI Cyborg v1.2.0 配置`,
      `# 由 Web 配置向导自动生成`,
      ``,
      `# AI 后端`,
      `AI_PROVIDER=${aiProvider || 'openai'}`,
      `AI_API_KEY=${aiApiKey || ''}`,
      `AI_MODEL=${aiModel || 'gpt-4o-mini'}`,
      `AI_BASE_URL=${aiBaseUrl || 'https://api.openai.com/v1'}`,
      ``,
      `# Minecraft 服务器`,
      `MC_HOST=${mcHost || 'localhost'}`,
      `MC_PORT=${mcPort || 25565}`,
      `MC_USERNAME=${mcUsername || 'AI_Cyborg'}`,
      `MC_VERSION=${mcVersion || '1.20.1'}`,
      `MC_AUTH=offline`,
      ``,
      `# 其他设置（使用默认值）`,
      `AI_MAX_CONTEXT=20`,
      `AI_MAX_TOKENS=8000`,
      `AI_TEMPERATURE=0.7`,
      `AI_ACTION_CHAIN=true`,
      `AI_VISION=false`,
      `AI_AUTONOMOUS=false`,
      `AI_MAX_TOOL_LOOPS=5`,
      `AI_PERSONA=你是一个友善的Minecraft AI赛博人。`,
      ``,
      `# Web 服务`,
      `WEB_PORT=3000`,
      `WEB_HOST=0.0.0.0`,
      `LOG_LEVEL=info`,
    ].join('\n');

    const envPath = path.join(process.cwd(), '.env');
    fs.writeFileSync(envPath, envContent, 'utf-8');

    getLogger().info('Config saved via web wizard');
    res.json({ success: true, message: '配置已保存，请重启服务生效' });
  } catch (e: any) {
    getLogger().error('Failed to save config:', e);
    res.status(500).json({ success: false, error: e.message });
  }
});
```

- [ ] **Step 5: 编译前端验证**

```bash
cd frontend && npx tsc --noEmit
```

---

### Task 3: 云平台一键部署

**Files:**
- Create: `deploy/railway.json`
- Create: `deploy/render.yaml`

**目标：** 提供一键部署按钮，用户点击即可在 Railway/Render 上启动

- [ ] **Step 1: Railway 部署配置**

```json
// deploy/railway.json
{
  "schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "node dist/index.js",
    "healthcheckPath": "/api/health",
    "restartPolicyType": "ON_FAILURE"
  }
}
```

- [ ] **Step 2: Render 部署配置**

```yaml
# deploy/render.yaml
services:
  - type: web
    name: mc-ai-cyborg
    env: docker
    dockerfilePath: ./Dockerfile
    healthCheckPath: /api/health
    envVars:
      - key: AI_PROVIDER
        value: free
      - key: AI_API_KEY
        sync: false
      - key: MC_HOST
        value: localhost
      - key: WEB_PORT
        value: "3000"
      - key: WEB_HOST
        value: "0.0.0.0"
```

- [ ] **Step 3: 在 README 中添加一键部署按钮**

```markdown
## 一键部署

[![Deploy on Railway](https://railway.app/button.svg)](https://railway.app/template/...)
[![Deploy to Render](https://render.com/images/deploy-to-render-button.svg)](https://render.com/deploy?repo=https://github.com/Annieif/cyborg)
```

---

### Task 4: 文档重写（面向零基础）

**Files:**
- Create: `QUICKSTART.md`
- Modify: `README.md` (开头部分重写)

**目标：** 零基础用户 3 分钟能上手

- [ ] **Step 1: 创建 QUICKSTART.md**

```markdown
# 快速开始（3 步）

## 第 1 步：安装 Docker

- **Windows/Mac**: 下载 [Docker Desktop](https://www.docker.com/products/docker-desktop/)
- **Linux**: `curl -fsSL https://get.docker.com | sh`

安装后打开 Docker Desktop，确认右下角图标变绿。

## 第 2 步：下载并启动

```bash
# 下载项目
git clone https://github.com/Annieif/cyborg.git
cd cyborg

# 启动（首次会自动下载镜像）
docker-compose up
```

## 第 3 步：打开浏览器配置

1. 浏览器打开 `http://localhost:3000`
2. 按照引导配置 AI 和 Minecraft 服务器
3. 点击「启动赛博人」

就这么简单！赛博人会自动加入 Minecraft 服务器并开始工作。

## 没有 Docker 怎么办？

如果你有 Node.js 18+，也可以直接运行：

```bash
npm install
npm run build
cp .env.example .env
# 编辑 .env 填入你的配置
npm start
```

## 常见问题

| 问题 | 解决 |
|------|------|
| 端口被占用 | 修改 docker-compose.yml 中的 `3000:3000` 为 `3001:3000` |
| 连接不上服务器 | 检查 MC_HOST 是否正确，服务器是否开启了正版验证 |
| 没有 AI 响应 | 检查 API Key 是否正确，网络能否访问 AI API |
| 免费 API 用不了 | ChatAnywhere 免费 API 有每日请求次数限制，可能已用完 |
```

- [ ] **Step 2: 重写 README 开头部分**

将 README.md 的「快速开始」章节替换为强调 Docker 一键部署和 Web 配置向导的内容。

---

### Task 5: 版本更新与发布

**Files:**
- Modify: `package.json` — version → `1.2.0`
- Modify: `.env.example` — 标注可省略项
- Modify: `task_plan.md` — 阶段12
- Modify: `progress.md` — 会话记录

- [ ] **Step 1: 更新版本号**

```json
// package.json
"version": "1.2.0"
```

- [ ] **Step 2: 更新 .env.example 注释**

在所有有默认值的配置项后添加 `# 可省略，有默认值`

- [ ] **Step 3: 编译验证**

```bash
npx tsc --noEmit
cd frontend && npx tsc --noEmit
```

- [ ] **Step 4: Docker 构建验证**

```bash
docker build -t annieif/cyborg:1.2.0 .
docker run --rm -p 3000:3000 -e AI_PROVIDER=free -e MC_HOST=localhost annieif/cyborg:1.2.0
# 验证: curl http://localhost:3000/api/health
```

- [ ] **Step 5: 更新 task_plan.md**

```markdown
| 阶段12: v1.2.0 零基础版 | ✅ complete | Docker一键部署 + Web配置向导 + 云平台部署 + 文档重写 |
```

- [ ] **Step 6: 更新 progress.md**

```markdown
## 会话 6: 2026-07-22 — v1.2.0 零基础版
### 完成事项
- Docker 一键部署增强（内置默认值）
- Web 配置向导（5 步引导式配置）
- 云平台一键部署模板（Railway + Render）
- 文档重写（QUICKSTART.md + README 开头）
- 编译验证通过
```

---

### Task 6: 可执行文件打包（无 Docker / 无 Node.js 用户）

**Files:**
- Create: `scripts/bundle-assets.js`
- Create: `scripts/build-binary.sh`
- Create: `scripts/build-binary.ps1`
- Create: `.github/workflows/release-binaries.yml`
- Modify: `src/server/index.ts` — 内存静态文件服务
- Modify: `src/index.ts` — 二进制模式检测
- Modify: `.gitignore` — 忽略生成文件

**目标：** 用户下载单个 .exe / 二进制 文件，双击即可运行，无需安装 Node.js 或 Docker

- [ ] **Step 1: 前端资源打包脚本**

```javascript
// scripts/bundle-assets.js
// 读取 frontend/dist/ 所有文件，生成 TypeScript 模块嵌入到二进制中
const fs = require('fs');
const path = require('path');

const DIST_DIR = path.join(__dirname, '..', 'frontend', 'dist');
const OUTPUT = path.join(__dirname, '..', 'src', 'frontendAssets.ts');

const mimeTypes = {
  '.html': 'text/html; charset=utf-8',
  '.css': 'text/css; charset=utf-8',
  '.js': 'application/javascript; charset=utf-8',
  '.json': 'application/json; charset=utf-8',
  '.png': 'image/png',
  '.svg': 'image/svg+xml',
  '.ico': 'image/x-icon',
  '.woff': 'font/woff',
  '.woff2': 'font/woff2',
};

function walkDir(dir, base = '') {
  const files = [];
  for (const entry of fs.readdirSync(dir)) {
    const fullPath = path.join(dir, entry);
    const relPath = base ? `${base}/${entry}` : entry;
    if (fs.statSync(fullPath).isDirectory()) {
      files.push(...walkDir(fullPath, relPath));
    } else {
      files.push({ relPath, fullPath });
    }
  }
  return files;
}

function generate() {
  if (!fs.existsSync(DIST_DIR)) {
    console.log('frontend/dist/ not found, creating empty asset map');
    fs.writeFileSync(OUTPUT, 'export const frontendAssets: Map<string, { content: Buffer; contentType: string }> = new Map();\n');
    return;
  }

  const files = walkDir(DIST_DIR);
  const entries = [];

  for (const { relPath, fullPath } of files) {
    const ext = path.extname(relPath).toLowerCase();
    const contentType = mimeTypes[ext] || 'application/octet-stream';
    const content = fs.readFileSync(fullPath);
    const base64 = content.toString('base64');
    entries.push(`  assets.set('${relPath.replace(/\\/g, '/')}', { content: Buffer.from('${base64}', 'base64'), contentType: '${contentType}' });`);
  }

  const output = [
    '// Auto-generated by scripts/bundle-assets.js — DO NOT EDIT',
    '// Contains all frontend/dist/ files for standalone binary mode',
    '',
    'export interface AssetEntry {',
    '  content: Buffer;',
    '  contentType: string;',
    '}',
    '',
    'export function getFrontendAssets(): Map<string, AssetEntry> {',
    '  const assets = new Map<string, AssetEntry>();',
    ...entries,
    '  return assets;',
    '}',
    '',
  ].join('\n');

  fs.writeFileSync(OUTPUT, output);
  console.log(`Bundled ${files.length} frontend assets into ${OUTPUT}`);
}

generate();
```

- [ ] **Step 2: 修改 WebServer 支持内存静态文件**

```typescript
// src/server/index.ts — 修改 setupMiddleware()
import { getFrontendAssets } from '../frontendAssets';

private setupMiddleware(): void {
  this.app.use(cors());
  this.app.use(express.json());

  // 检测是否为编译后的二进制（无 frontend/dist 目录）
  const fs = require('fs');
  const hasFrontendDist = fs.existsSync('frontend/dist');

  if (hasFrontendDist) {
    // 开发/Docker 模式：从磁盘读取
    this.app.use(express.static('frontend/dist'));
  } else {
    // 二进制模式：从内存读取
    const assets = getFrontendAssets();
    this.app.use((req, res, next) => {
      // 只处理非 API 请求
      if (req.path.startsWith('/api/')) return next();

      let filePath = req.path === '/' ? '/index.html' : req.path;
      filePath = filePath.replace(/^\//, '');

      const asset = assets.get(filePath);
      if (asset) {
        res.setHeader('Content-Type', asset.contentType);
        res.send(asset.content);
      } else {
        // SPA fallback: 返回 index.html
        const indexAsset = assets.get('index.html');
        if (indexAsset) {
          res.setHeader('Content-Type', indexAsset.contentType);
          res.send(indexAsset.content);
        } else {
          next();
        }
      }
    });
  }
}
```

> **注意**：`import { getFrontendAssets }` 在编译为二进制时必须存在。开发模式下 `bundle-assets.js` 会生成一个空 Map 的 fallback。

- [ ] **Step 3: 添加 npm scripts**

```json
// package.json — 添加 scripts
{
  "scripts": {
    "bundle:assets": "node scripts/bundle-assets.js",
    "build:binary": "npm run build:frontend && npm run bundle:assets && npm run build && bun build ./dist/index.js --compile --outfile cyborg-bot",
    "build:binary:win": "npm run build:frontend && npm run bundle:assets && npm run build && bun build ./dist/index.js --compile --target bun-linux-x64-modern --outfile cyborg-bot-linux && bun build ./dist/index.js --compile --target bun-windows-x64-modern --outfile cyborg-bot.exe && bun build ./dist/index.js --compile --target bun-darwin-x64-modern --outfile cyborg-bot-mac"
  }
}
```

- [ ] **Step 4: 构建脚本**

```bash
#!/bin/bash
# scripts/build-binary.sh
# 一键构建当前平台的可执行文件
set -e

echo "=== 构建 Minecraft AI 赛博人 可执行文件 ==="

# 1. 安装依赖
echo "[1/4] 安装依赖..."
npm ci --legacy-peer-deps
cd frontend && npm ci && cd ..

# 2. 构建前端
echo "[2/4] 构建前端..."
cd frontend && npm run build && cd ..

# 3. 打包前端资源 + 编译后端
echo "[3/4] 打包资源 + 编译 TypeScript..."
node scripts/bundle-assets.js
npx tsc

# 4. 编译为可执行文件
echo "[4/4] 编译为可执行文件..."
if command -v bun &> /dev/null; then
  bun build ./dist/index.js --compile --outfile cyborg-bot
  echo "✓ 完成: ./cyborg-bot"
  
  # 检查大小
  SIZE=$(du -h cyborg-bot | cut -f1)
  echo "  文件大小: $SIZE"
else
  echo "需要 Bun 来编译可执行文件。安装: curl -fsSL https://bun.sh/install | bash"
  exit 1
fi
```

```powershell
# scripts/build-binary.ps1 (Windows)
Write-Host "=== 构建 Minecraft AI 赛博人 可执行文件 ==="

# 1. 安装依赖
Write-Host "[1/4] 安装依赖..."
npm ci --legacy-peer-deps
cd frontend; npm ci; cd ..

# 2. 构建前端
Write-Host "[2/4] 构建前端..."
cd frontend; npm run build; cd ..

# 3. 打包资源 + 编译
Write-Host "[3/4] 打包资源 + 编译 TypeScript..."
node scripts/bundle-assets.js
npx tsc

# 4. 编译为可执行文件
Write-Host "[4/4] 编译为可执行文件..."
bun build ./dist/index.js --compile --outfile cyborg-bot.exe
Write-Host "✓ 完成: ./cyborg-bot.exe"
$size = (Get-Item cyborg-bot.exe).Length / 1MB
Write-Host "  文件大小: $([math]::Round($size, 1)) MB"
```

- [ ] **Step 5: GitHub Actions 自动化构建**

```yaml
# .github/workflows/release-binaries.yml
name: Build Release Binaries

on:
  release:
    types: [published]
  workflow_dispatch:

jobs:
  build:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        include:
          - os: ubuntu-latest
            artifact: cyborg-bot-linux
            ext: ''
          - os: windows-latest
            artifact: cyborg-bot.exe
            ext: .exe
          - os: macos-latest
            artifact: cyborg-bot-mac
            ext: ''

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v1

      - name: Install dependencies
        run: |
          npm ci --legacy-peer-deps
          cd frontend && npm ci && cd ..

      - name: Build frontend
        run: cd frontend && npm run build && cd ..

      - name: Bundle assets & compile TypeScript
        run: |
          node scripts/bundle-assets.js
          npx tsc

      - name: Compile binary
        run: bun build ./dist/index.js --compile --outfile ${{ matrix.artifact }}

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: ${{ matrix.artifact }}

      - name: Attach to release
        if: github.event_name == 'release'
        uses: softprops/action-gh-release@v1
        with:
          files: ${{ matrix.artifact }}
```

- [ ] **Step 6: 更新 .gitignore**

```
# 生成的可执行文件
cyborg-bot
cyborg-bot.exe
cyborg-bot-linux
cyborg-bot-mac

# 自动生成的前端资源文件
src/frontendAssets.ts
```

- [ ] **Step 7: 创建默认空 frontendAssets.ts（开发模式 fallback）**

```typescript
// src/frontendAssets.ts (开发模式默认版本，构建时由 bundle-assets.js 覆盖)
export interface AssetEntry {
  content: Buffer;
  contentType: string;
}

export function getFrontendAssets(): Map<string, AssetEntry> {
  // 开发模式下从磁盘读取，此函数仅作为 fallback
  return new Map();
}
```

- [ ] **Step 8: 验证构建**

```bash
# 在项目根目录
npm run build:binary

# 运行
./cyborg-bot
# 打开 http://localhost:3000 验证前端页面正常加载
```

---

## 自审清单

### 1. Spec 覆盖
- [x] 零基础用户无需安装 Node.js → Docker 一键部署
- [x] 无 Docker 用户 → 可执行文件（.exe / 二进制）双击运行
- [x] 无需编辑 .env 文件 → Web 配置向导
- [x] 一键部署到云平台 → Railway/Render 模板
- [x] 文档面向零基础 → QUICKSTART.md + README 重写
- [x] 自动化多平台发布 → GitHub Actions 构建 win/mac/linux 二进制

### 2. 占位符检查
- [x] 无 TBD/TODO/占位符
- [x] 所有代码步骤包含完整代码
- [x] 所有命令包含预期结果

### 3. 类型一致性
- [x] SetupConfig 接口在所有步骤中一致
- [x] API 路由命名一致：/api/config/check, /api/config/save
- [x] docker-compose.yml 环境变量与 config schema 一致